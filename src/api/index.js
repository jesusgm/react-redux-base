var config = require('../config');

/**Example api calls */
/*
export const getCall = () =>
  fetch(`${config.API_PATH}/relativeurl/`).then(response =>
    response.json()
  );

export const getByIdCall = (id) =>
  fetch(`${config.API_PATH}/relativeurl/${id}`).then(response =>
    response.json()
  );

export const putCall = (id, data) => {
  return fetch(`${config.API_PATH}/relativeurl/${id}`, {
      method: 'PUT',
      body: JSON.stringify(data),
      mode: 'cors',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json'
      }
    })
    .then(response => response.json());
}

*/